app:
  datasource:
    #driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
flyway:
  baselineOnMigrate: true
spring:
  main:
    banner-mode: off